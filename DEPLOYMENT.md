# 认词簿部署说明

## 必需环境变量

公网部署前，必须在托管平台的服务端环境变量或 Secret 中配置：

```text
ACCESS_CODE=<家庭访问码>
```

真实访问码不要写入代码、`.env.example`、构建参数或公开仓库。当前约定值记录在本机的 `DEPLOYMENT.local.md`，该文件已被 Git 忽略。

本地开发使用 `.env.local`：

```text
ACCESS_CODE=<家庭访问码>
```

修改访问码后，之前签发的登录 Cookie 会自动失效，所有设备需要重新验证。

## 部署后检查

1. 未登录访问首页时，应自动跳转到 `/login`。
2. 错误访问码应返回“访问码不正确”。
3. 正确访问码登录后，应能进入认词簿。
4. 在“学习记录”中选择“退出当前设备”后，应重新回到登录页。
5. 公网环境必须使用 HTTPS，以启用 `Secure` Cookie。

## 数据说明

- 学习记录目前保存在浏览器本地存储中，不会在不同设备之间自动同步。
- 其他访问者无法修改孩子当前设备上的数据。
- 清除浏览器数据会清除学习记录，建议定期导出 CSV 备份。

## 安全说明

- 服务端连续输错 5 次会锁定 10 分钟。
- 四位访问码便于家庭使用，但安全强度有限；如果站点地址被广泛传播，建议换成更长的字母数字组合。
- 访问码只能通过托管平台的 Secret/环境变量配置，不能打包进前端。
